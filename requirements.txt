# Core dependencies -- used today by src/hydro_flow_ai/reconstruct_dataset.py
numpy>=1.24
pandas>=2.0

# --- Planned, not yet used in code -----------------------------------------
# Uncomment as each stage is implemented, or install via:
#   pip install -e ".[ml]"
#   pip install -e ".[dl]"
#   pip install -e ".[dev]"
#
# xgboost>=2.0
# scikit-learn>=1.3
# torch>=2.1
# pytest>=8.0
# ruff>=0.5
